#include "arvore.h"
#include <iostream>

int main() {
    Arvore arvore;

    arvore.inserir(10);
    arvore.inserir(5);
    arvore.inserir(20);
    arvore.inserir(3);
    arvore.inserir(7);
    arvore.inserir(15);
    arvore.inserir(25);



    system("pause");
    return 0;
}
